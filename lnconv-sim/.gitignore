/target
Cargo.lock
.serena/

# Per-run stats output (auto-named: {topology}-{algo}-{event}-{stamp}.parquet)
/*.parquet
/sim_output

# Profiler artifacts (samply JSON, flamegraph SVG, etc.) — see PERF.md.
/perf